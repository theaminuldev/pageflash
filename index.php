<?php
// Silence is golden.
//  "zip": "npm run build && npm run prod && mv build pageflash && zip -r pageflash.$npm_package_version.zip pageflash/*",
// "zip": "npm run build && npm run prod && move build pageflash && zip -r pageflash.zip pageflash/*",

echo 'Hello World';